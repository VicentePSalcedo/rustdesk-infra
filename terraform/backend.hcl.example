# Copy to backend.hcl and fill in. Never commit backend.hcl.
# Bucket is created once via scripts/bootstrap-backend.sh.

bucket       = "rustdesk-terraform-state-REPLACE_WITH_ACCOUNT_ID"
key          = "rustdesk/terraform.tfstate"
region       = "us-east-1"
use_lockfile = true
encrypt      = true
